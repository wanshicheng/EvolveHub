# ==============================================================
# EvolveHub Admin Service - Development Profile
# ==============================================================
server:
  port: 8083

spring:
  # ----- DataSource (local PostgreSQL, 与 auth 共用同一库) -----
  datasource:
    url: jdbc:postgresql://106.15.231.177:${DB_PORT:5432}/evolve_auth
    username: admin
    password: a774461674
    driver-class-name: org.postgresql.Driver

  # ----- Redis (shared token store with auth) -----
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0

# ----- Logging (development: show SQL) -----
logging:
  level:
    org.evolve: DEBUG
    com.baomidou.mybatisplus: DEBUG
